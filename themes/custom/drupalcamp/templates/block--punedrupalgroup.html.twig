<div class="container">
  <div class="row">
    <div class="col-sm-12">
      {% if label %}
        <h2{{ title_attributes }}>{{ label }}</h2>
      {% endif %}

      {{ content.body }}
      </div>
  </div>
</div>
